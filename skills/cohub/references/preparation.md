# Creative Capability Preparation and Acceptance

Read only the selected capability. Open its direct source from the main entry first. This table records known dependencies, not a substitute for current package commands. Never invent installers, file lists, health checks or paths.

| Capability | Required content and dependencies | Low-risk readiness check | Acceptance |
|---|---|---|---|
| Style | AGENT_GUIDE.md and adjacent catalog.json; generation uses the generation module | Parse catalog.json and verify the selected entry and references are readable | Record the selected style; verify generated media separately |
| Game Maker | Selected subskill and its referenced runtime, assets, templates, tools and siblings; establish Neta CLI, generation, cutout and CDN needs from that entry | Inspect manifests and scripts, check runtime/dependency versions; run only inspected checks without paid tasks or remote writes | Game starts, key interactions and assets work; verify publication separately |
| Fandom | Current entry's HTTP APIs, required authentication and references; no invented Fandom CLI | Establish the contract, then an authorized read-only query supported by the entry | Relevant sourced content and usable image references |
| OKP search | Independent CLI/auth and domain schema required by the current entry | Installed help, documented read-only identity check and schema read | Sourced matching results; no imports, writes or exports |
| Character Traits | Entry's referenced data, scripts and dependencies; resolve legacy ~/.claude paths | Check referenced files, inspect scripts before using a provided side-effect-free validator; disclose when none exists | Requested personality, contradictions and arc consistent with the user's existing character |

## Acquire and Execute

- Materialize files only when package execution requires them. Use a confirmed unoccupied task-specific project directory, not another globally installed skill or an overwritten project. Record the actual package root.
- Establish required files from the current entry, manifests and relative references. Preserve structure and resolve old absolute paths. If the dependency closure is unclear, report missing items; an entry file alone is not readiness.
- Record Space ID, entry path, read time and a content digest; include upstream version/snapshot ID when provided. Prefer one snapshot. If unavailable, record key file digests and recheck the entry after acquisition; reconcile changes rather than claiming a fixed version for mixed content.
- Record commands, inputs, dependencies and acceptance checks. Stop research when the current step is established; reuse applicable evidence from this task.
- Dependency installation, script execution, paid generation and publication follow their respective authorization. Inspect scripts first. Denied access blocks that source only, not independent work; do not change permissions or substitute restricted sources.
- Report source access, complete files, dependency readiness, execution, usable output and visitor-path validation separately. Never infer untested stages or create paid tasks just for readiness checks.
